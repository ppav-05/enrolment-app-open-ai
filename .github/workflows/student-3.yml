name: CI/CD Pipeline - Student 3

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'student-3/**'
      - 'docker-compose.yml'
      - '.github/workflows/student-3.yml'
  pull_request:
    branches: [ main, develop ]
    paths:
      - 'student-3/**'
      - 'docker-compose.yml'

env:
  REGISTRY: ghcr.io
  IMAGE_NAMESPACE: ${{ github.repository_owner }}
  STUDENT_NUM: 3
  FRONTEND_PORT: 8003
  BACKEND_PORT: 5004
  DATABASE_PORT: 5012

jobs:
  build:
    name: Build Docker Images
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        if: github.event_name == 'push'
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/metadata-action@v5
        id: meta-frontend
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAMESPACE }}/student-3-frontend
          tags: type=ref,event=branch;type=sha

      - uses: docker/metadata-action@v5
        id: meta-backend
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAMESPACE }}/student-3-backend
          tags: type=ref,event=branch;type=sha

      - uses: docker/metadata-action@v5
        id: meta-database
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAMESPACE }}/student-3-database
          tags: type=ref,event=branch;type=sha

      - name: Build images
        run: |
          for service in frontend backend database; do
            if [ -f "student-3/$service/Dockerfile" ]; then
              echo "Building $service..."
              docker buildx build --load -t student-3-$service:${{ github.sha }} ./student-3/$service
              echo "✅ $service built"
            fi
          done

  validate:
    name: Validate Configuration
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker compose config > /dev/null && echo "✅ Valid"

  report:
    name: Generate Report
    runs-on: ubuntu-latest
    needs: [ build, validate ]
    if: always()
    steps:
      - uses: actions/checkout@v4
      - run: |
          cat > report.md << EOF
          # Student 3 Build Report
          - Date: $(date)
          - Commit: ${{ github.sha }}
          - Status: ✅ Complete
          EOF
      - uses: actions/upload-artifact@v4
        with:
          name: build-report-student-3
          path: report.md
